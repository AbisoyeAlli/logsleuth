# =============================================================================
# LogSleuth Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# ELASTICSEARCH CONNECTION
# Get these from your Elastic Cloud deployment: https://cloud.elastic.co
# -----------------------------------------------------------------------------

# Option 1: URL + API Key (Recommended)
ELASTICSEARCH_URL=https://your-deployment.es.us-east-1.aws.elastic.cloud:443
ELASTICSEARCH_API_KEY=your-es-api-key-here

# Option 2: Cloud ID + API Key
# ELASTICSEARCH_CLOUD_ID=my-deployment:dXMtZWFzdC0xLmF3cy5mb3VuZC5pbyQ...
# ELASTICSEARCH_API_KEY=your-es-api-key-here

# Option 3: URL + Username/Password (for local/dev)
# ELASTICSEARCH_URL=https://localhost:9200
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=your-password

# -----------------------------------------------------------------------------
# KIBANA / AGENT BUILDER CONNECTION
# Agent Builder is accessed via Kibana API endpoints
# Find your Kibana URL in Elastic Cloud console under your deployment
# -----------------------------------------------------------------------------

KIBANA_URL=https://your-deployment.kb.us-east-1.aws.elastic.cloud:9243
KIBANA_API_KEY=your-kibana-api-key-here

# To generate a Kibana API key:
# 1. Open Kibana in browser
# 2. Go to Stack Management → API Keys (or search "API keys")
# 3. Click "Create API key" → name it "logsleuth"
# 4. Copy the encoded key

# -----------------------------------------------------------------------------
# INDEX CONFIGURATION
# -----------------------------------------------------------------------------

LOG_INDEX=logs-logsleuth
INVESTIGATION_INDEX=investigations-logsleuth

# -----------------------------------------------------------------------------
# LLM CONFIGURATION (Optional - Agent Builder uses Elastic Managed LLM by default)
# -----------------------------------------------------------------------------

# If using a custom LLM connector:
# LLM_CONNECTOR_ID=your-connector-id
